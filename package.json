{
  "name": "pioneer-portal-v3",
  "scripts": {
    "dev": "npm run dev --prefix server",
    "start": "npm run start --prefix server",
    "install-all": "npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}