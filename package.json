{
  "name": "pioneer-portal-v3",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"tsx server/server.ts\"",
    "install-all": "npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}